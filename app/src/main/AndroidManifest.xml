<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agricultural_products_store">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Agricultural_products_store">
        <activity android:name=".DetaillReplyCommentActivity"></activity>
        <activity android:name=".DetailReplyCommentActivity" />
        <activity android:name=".DetailOrderActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".DetailPaymentActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".profile.ProfileUserActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name="Screen.SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>